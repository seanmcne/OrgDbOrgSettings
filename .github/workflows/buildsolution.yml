name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: windows-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          dir ${{ github.workspace }}
      - name: List files in the repository dir we want to see recurse
        run: |
          dir ${{ env.ARTIFACT_DIR  }} -Recurse
      - name: List files in the repository dir we want to see
        run: |
          $root = $env:GITHUB_WORKSPACE
          write-host $root
          $path = "\mspfedyn_\OrgDbOrgSettings\Solution"
          $fullpath = Join-Path -Path $root -ChildPath $path
          dir $fullpath
          echo "fullpath=$fullpath" >> $env:VAR_FULLPATH
          $currentPath = Get-Location
          echo "CurrentPath=$currentPath" >> $env:GITHUB_ENV
      - run: echo "🍏 This job's status is ${{ job.status }}."
      - name: Install Power Platform Tools
        uses: microsoft/powerplatform-actions/actions-install@v1
      - run: |
          echo "Using CurrentPath: $env:CurrentPath"
      - name: Pack Solution
        uses: microsoft/powerplatform-actions/pack-solution@v1
        with:
          solution-file: "$env:VAR_FULLPATH\\something.zip"
          solution-folder: $env.VAR_FULLPATH
          solution-type: "both"
